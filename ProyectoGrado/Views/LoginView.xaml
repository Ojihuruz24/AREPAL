<Window x:Class="ProyectoGrado.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProyectoGrado.Views"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             Width="300" Height="350"
             Block.TextAlignment="Center"
             >
    <Window.Resources>
        <Style TargetType="TextBox" BasedOn="{StaticResource BaseTextBox}"/>
        <Style TargetType="PasswordBox" BasedOn="{StaticResource BasePasswordBox}"/>
    </Window.Resources>
    <Border>
        <Grid Background="Bisque">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Grid.RowSpan="2">
                <Label Content="USUARIO" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" />
                <TextBox Text="{Binding User, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Bottom" TextAlignment="Center" />
            </Grid>
            <Grid Grid.RowSpan="2" Grid.Row="3">
                <Label Content="CONTRASEÑA" Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Top"  Width="90"/>
                <PasswordBox Grid.Row="4" 
                     PasswordChanged="OnPasswordChanged"
                     Tag="{Binding Password, UpdateSourceTrigger=PropertyChanged}"
                     VerticalAlignment="Center"/>
            </Grid>
            <Button Content="INGRESAR" Grid.Row="5" Width="120" Height="40" Command="{Binding Path=LoginUserCommand}"/>
        </Grid>
    </Border>
</Window>
